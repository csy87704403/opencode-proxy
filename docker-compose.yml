version: '3.8'

services:
  opencode-proxy:
    build: .
    container_name: opencode-proxy
    restart: unless-stopped
    ports:
      - "20128:20128"
    environment:
      - PORT=20128
      - PROXY_API_KEY=sk_opencode_proxy_default_key_2026
