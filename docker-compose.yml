version: '3.8'

services:
  opencode-proxy:
    build: .
    container_name: opencode-proxy
    restart: unless-stopped
    ports:
      - "20128:20128"
    environment:
      - PORT=20128
      - BLACKLIST_MODELS=minimax-m3-free,minimax-m2.5-free,qwen3.6-plus-free,trinity-large-preview-free
